# 静态扫雷计算器 (Minesweeper Static Solver)

> 基于穷举算法的静态扫雷逻辑推理工具
## 📖 简介

**纸上扫雷逻辑求解器**是一个专门用于解决**静态扫雷谜题**的 Python 工具。与传统的交互式扫雷游戏不同，本工具接收一个静态的扫雷盘面（包含已知数字和未翻开方格），通过**穷举**和**DFS**，计算出雷的位置

### 核心特点

额前面忘了后面忘了，反正随便用，不依赖任何库

### 使用方法(可参考"快速使用.py")

可能需要一点点编程基础

```python
from 扫雷计算器 import 计算, 显示全部结果 , 显示整合结果

#   由于input输入换行比较麻烦，可以在input.txt里输入内容
with open('input.txt', 'r', encoding='utf-8') as f:
    txt = f.read()
print(txt)    # 要计算的棋盘
显示全部结果(txt)
# 显示整合结果(txt)
```
### 运行结果
```text
1 ? ? ? 1 1 ? ? ? 1
? ? 2 2 ? ? 2 3 ? ?
2 ? ? 2 3 ? ? 2 ? ?
? ? ? ? ? ? ? ? ? 2
? 2 3 ? 6 ? 6 ? 3 ?
? ? ? ? ? 6 ? ? ? ?
? 3 ? 3 ? ? ? 4 2 1
2 ? 1 2 ? ? 3 ? ? ?
? ? ? ? 2 2 ? ? ? 3
? 2 2 ? ? 1 ? 2 2 ?
预处理结果:
  数字格(1-8): 36 个
  零格(0): 0 个
  第1个数字格: 位置(0,0), 数字=1, 周边候选格数=3
  第2个数字格: 位置(0,4), 数字=1, 周边候选格数=3
  ...
  第36个数字格: 位置(9,8), 数字=2, 周边候选格数=3
    位置(0,0)数字1: 3种局部排布
    位置(0,4)数字1: 3种局部排布
    ...
    位置(9,8)数字2: 3种局部排布
暴力求解ing...
搜索完成！
  总检验次数: 104607
  剪枝次数: 97869
  剪枝率: 93.6%
  含孤立格总合理排列: 1 种（约束区独立排布共1种）
==================================================
总排布数量: 1 种（约束区独立排布 1 种）
==================================================
========== 排布方案 1 ==========
1 · ● · 1 1 · ● ● 1
● · 2 2 ● · 2 3 · ·
2 ● · 2 3 · ● 2 · ●
· · · · ● ● ● · · 2
· 2 3 ● 6 ● 6 ● 3 ●
● ● · ● ● 6 ● ● · ·
● 3 · 3 ● ● ● 4 2 1
2 · 1 2 · ● 3 · ● ·
● · ● · 2 2 · · ● 3
· 2 2 ● · 1 ● 2 2 ●
```

### 安装

```bash
git clone https://github.com/WannaBeCute/Minesweeper-Static-Solver.git
```

## AIGC 声明
> 本项目代码由 AI 辅助生成  This project code is AI-assisted generated.
### 关于 AI 生成

本项目的核心代码由 AI 辅助生成，提示词已附在 `提示词.txt` 中。
本项目采用 MIT 协议开源，代码可完全自由使用。作者仅仅只是知识分享~
