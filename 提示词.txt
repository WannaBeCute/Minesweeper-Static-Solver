#简单案例
test_01= """
口口③
口口口
口②口
"""
#错误案例
test_02 = """
口口③
口口口
②②口
"""
#复杂案例
test_03 = """
口③口②
口口口口
口口口①
口③口口
"""


def 转换为矩阵(data: str, 映射字符: str = ''):
    mat_str = data.strip().replace(' ', '')
    if 映射字符:    #不为空时则进行映射
        if len(set([i for i in 映射字符])) != 10:
            return print("映射字符错误！请仔细检查！")
        else:
            映射表, 目标字符 = {}, '?012345678'
            for i in range(9):
                映射表[映射字符[i]] = 目标字符[i]
            mapping = str.maketrans(映射表)
            mat_str = mat_str.translate(mapping)  # 翻译为可供程序识别的矩阵
    mat = [[char for char in lst] for lst in mat_str.split('\n')]
    return mat

#   如果一定为雷则输出●,如果一定不为雷且周围雷的数量确定则输出周围雷的数量,如果一定不为雷但不确定周围雷的数量则输出
#   √,如果不确定是否有雷则输出!,如果还没进行判断则保留?,如果周围没有雷则为0,最后的计算结果不能含有?
mat = 转换为矩阵(test_01)    #以左上角为坐标原点(0,0)，(a,b)代表第b+1行第a+1列,对应mat[b][a]

    
    

计算扫雷结果(mat)
我想写一个扫雷的静态计算代码，基于穷举算法，不过这个还没写完，请你不要以自己的逻辑续写，我需要你用我的逻辑续写

请你根据上面的数据结构，写一个预处理()函数和合理性校验()函数，预处理函数是对每个含数字方格进行数据标注(不包括空白方格’?‘)，要计算一个数字方格周围可能有雷的方格（第一次计算公式：有效方格数量(处理边界方格时有效方格不是8，因此要具体处理)-数字方格数量），以及这个数字方格周围暂时无法确定位置的雷的数量(第一次计算即数字方格里的数字)。输出结构为列表且只包含数字方格的数据，记得加上坐标信息（元组tuple存储因为无法改变）
排列情况转换函数，作用是识别二进制数从而实现排列，一个数字方格周围最多只有8个方格，雷的所有排列(包括无雷)总共有2**8种，规定方格上方为从右到左第一位，顺时针顺位，如果有雷则为1无雷则为0，如果周围不足8个方格则将缺少的位数也用0补上，比如test_01中的3只有一种排列即0(上)0001(下)1(左下)1(左)0
合理性校验函数，需要计算基于数据(会包含雷的位置数据和数字方格数据)计算当前排列是否错误。计算方式是通过每个数字方格数据存储的2进制排列情况，计算所有雷的位置，如果发现某数字方格周围的雷已经超过该数字周围确定可能有雷的方格数量或是超过该数字（即周围有效方格数-周围确定百分百无雷数）。如果这个数据没有问题则输出True否则False

然后再写一个复杂遍历，遍历数字方格数据，通过数据计算当前数字方格周围雷的排列情况，再遍历所有的雷的排列情况，每个遍历情况都进行一次合理性校验，然后用if函数判断排列是否错误，错误的直接删除此排列，正确的保留该排列，一次遍历排列要保留合理的排列情况(用2进制存储)
然后遍历所有的数字方格数据，筛出所有的排列情况(从二进制数据和数字方格数据转换为矩阵)，并输出

————————————————————————————————————————————————————————————————————————————————————————————————————————————————————
上述的代码可以生成一个正确的扫雷计算程序，但是我自己生成的时候处理数字0会显示错误，如果程序计算数字0时出现问题，请你用下面的提示词进行修改
注意对数字0的特殊处理，数字0代表周围方格雷的数量为0，在扫雷游戏里表现为空白的已开出的方块，坐标处理时需和其他数字(1-8)分开处理